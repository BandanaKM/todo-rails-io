<%= render '/shared/topnavb' %> 

<br>
<br>
<br>
<br>

<div class="container"> 
    <div class="row">
      <div class="col-lg-12 text-left">
      <h1>Edit todo</h1>
      </div>
    </div>

<%= form_for(@todo) do |f| %>
  <% if @todo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo.errors.count, "error") %> prohibited this todo from being saved:</h2>

      <ul>
      <% @todo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

 <div class="row">
  <div class="col-md-12">
      <div class="field">
        <div class=".form-group-lg floating-label-form-group controls">
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
    	</div>
    </div>
    <div class="actions" class="btn btn-success btn-sm">
    <button type="submit" class="btn btn-success btn-large">Update</button>
<% end %>
<%= link_to 'Back', todos_path, :class => "btn btn-warning" %>
    </div>
  </div>

<br>
<br>

<%= render '/shared/footer' %> 